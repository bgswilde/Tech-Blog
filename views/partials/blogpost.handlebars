<div class="card">
  <div class="card-header">
    <h3>{{title}}</h3>
  </div>
  <div class="card-body">
    <p class="card-text">{{content}}}</p>
    <a href="#" class="btn btn-primary">View & Comment</a>
  </div>
  <div class="card-footer text-muted">
    <p> by {{user.username}} on {{format_date created_at}}  |  {{format_plural "comment" comments.length}}
  </div>
</div>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css"
        rel="stylesheet"
        integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We"
        crossorigin="anonymous"
	/>
    <link rel="stylesheet" href="./stylesheets/style.css">
</head>

<body>
    <!-- HEADER and NAV -->
    <div class="container">
        <header class="d-flex flex-wrap justify-content-center py-3 mb-4">
            <nav class="navbar fixed-top navbar-light bg-light">
                <a href="#" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto">
                    <img class="" src="./images/techblog-header.png" alt="" width="350">
                </a>
                <ul class="nav fs-5 custom-link">
                    <li class="nav-item">
                        <a class="nav-link custom-link" href="#">home</a>
                    </li>
                    {{#if loggedIn}}
                    <li class="nav-item">
                        <a class="nav-link custom-link" href="/dashboard">dashboard</a>
                    </li>
                    <li class="nav-item"> 
                        <a class="nav-link custom-link" href="/logout">logout</a>
                    </li>
                    {{else}}
                    <li class="nav-item"> 
                        <a class="nav-link custom-link" href="/login">login</a>
                    </li>
                    {{/if}}
                </ul>
            </nav>
        </header>
    </div>
    <div class="container content-area">
        <main>
        {{{ body }}}
        </main>
    </div>
{{#if loggedIn}} 
<script src="/javascript/logout.js"></script>
{{/if}}
</body>

</html>
